# dev-platform

```bash
sudo add-apt-repository -y ppa:git-core/ppa
sudo apt install -y git

mkdir -p ~/Projects/yvh
cd ~/Projects/yvh
git clone git clone git@github.com:yvh/dev-platform

./build-locale.sh # reboot after
./build-env.sh
```
